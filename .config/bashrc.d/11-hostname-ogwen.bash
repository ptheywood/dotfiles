# Owgwen specific rules 
# @todo - there may be a more yadm specific way to do this, but more portable dotfiles are probably better. (i.e. I can just copy things if needed.)
# if [ "$HOSTNAME" == "ogwen" ]; then
    
# fi
